import { HomePage } from '../components/pages/Home';
import { createStyles, Title, Text, Button, MantineProvider } from '@mantine/core';

<MantineProvider
    withGlobalStyles
    withNormalizeCSS
    theme={{
        colorScheme: 'dark'
    }}
>
    <HomePage />
</MantineProvider>
