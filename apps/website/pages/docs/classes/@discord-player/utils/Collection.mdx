## Collection extends Collection\<K, V>


```typescript
new Collection(entries)
```
| Parameter | Type | Optional |
| ----------- | ----------- | ----------- |
| entries | null \| readonly Array\<readonly \[K, V]> | ✅ |


## Properties
### private #array: [any](https://www.typescriptlang.org/docs/handbook/2/everyday-types.html#any)
- [Source](https://github.com/Androz2091/discord-player/blob/cd18f37/packages/utils/src/Collection.ts#L4)
### public \[toStringTag]: [any](https://www.typescriptlang.org/docs/handbook/2/everyday-types.html#any)

### public constructor: [any](https://www.typescriptlang.org/docs/handbook/2/everyday-types.html#any)

### public size: [any](https://www.typescriptlang.org/docs/handbook/2/everyday-types.html#any)

### public static \[species]: [any](https://www.typescriptlang.org/docs/handbook/2/everyday-types.html#any)


## Methods
### public \[iterator](): IterableIterator\<\[KV]>
Returns an iterable of entries in the map.
### public array(): [Array](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array)\<V>
- [Source](https://github.com/Androz2091/discord-player/blob/cd18f37/packages/utils/src/Collection.ts#L9)
### public at(index): undefinedV
Identical to Array.at().
Returns the item at a given index, allowing for positive and negative integers.
Negative integers count back from the last item in the collection.



| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| index | [number](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Number) | ❌ | The index of the element to obtain |
### public clear(): [void](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/undefined)

### public clone(): Collection\<KV>
Creates an identical shallow copy of this collection.

```ts
const newColl = someColl.clone();
```
### public concat(collections): Collection\<KV>
Combines this collection with others into a new collection. None of the source collections are modified.

```ts
const newColl = someColl.concat(someOtherColl, anotherColl, ohBoyAColl);
```

| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| collections | [Array](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array)\<ReadonlyCollection\<KV>> | ❌ | Collections to merge |
### public delete(key): [boolean](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Boolean)
| Parameter | Type | Optional |
| ----------- | ----------- | ----------- |
| key | K | ❌ |


- [Source](https://github.com/Androz2091/discord-player/blob/cd18f37/packages/utils/src/Collection.ts#L21)
### public difference(other): Collection\<KVT>
The difference method returns a new structure containing items where the key is present in one of the original structures but not the other.



| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| other | ReadonlyCollection\<KT> | ❌ | The other Collection to filter against |
### public each(fn): Collection\<KV>
Identical to
Map.forEach(),
but returns the collection instead of undefined.

```ts
collection
 .each(user => console.log(user.username))
 .filter(user => user.bot)
 .each(user => console.log(user.username));
```

| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| fn | (value: Vkey: Kcollection: Collection,\<,K,V,>) => [void](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/undefined) | ❌ | Function to execute for each element |
### public ensure(key, defaultValueGenerator): V
Obtains the value of the given key if it exists, otherwise sets and returns the value provided by the default value generator.

```ts
collection.ensure(guildId, () => defaultGuildConfig);
```

| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| key | K | ❌ | The key to get if it exists, or set otherwise |
| defaultValueGenerator | (key: Kcollection: Collection,\<,K,V,>) => V | ❌ | A function that generates the default value |
### public entries(): IterableIterator\<\[KV]>
Returns an iterable of key, value pairs for every entry in the map.
### public equals(collection): [boolean](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Boolean)
Checks if this collection shares identical items with another.
This is different to checking for equality using equal-signs, because
the collections may be different objects, but contain the same data.



| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| collection | ReadonlyCollection\<KV> | ❌ | Collection to compare with |
### public every(fn):  isCollection\<K2V>
Checks if all items passes a test. Identical in behavior to
Array.every().

```ts
collection.every(user => !user.bot);
```

| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| fn | (value: Vkey: Kcollection: Collection,\<,K,V,>) =>  isK2 | ❌ | Function used to test (should return a boolean) |
### public filter(fn): Collection\<K2V>
Identical to
Array.filter(),
but returns a Collection instead of an Array.

```ts
collection.filter(user => user.username === 'Bob');
```

| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| fn | (value: Vkey: Kcollection: Collection,\<,K,V,>) =>  isK2 | ❌ | The function to test with (should return boolean) |
### public find(fn): undefinedV2
Searches for a single item where the given function returns a truthy value. This behaves like
Array.find().
All collections used in Discord.js are mapped using their `id` property, and if you want to find by id you
should use the `get` method. See
MDN for details.

```ts
collection.find(user => user.username === 'Bob');
```

| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| fn | (value: Vkey: Kcollection: Collection,\<,K,V,>) =>  isV2 | ❌ | The function to test with (should return boolean) |
### public findKey(fn): undefinedK2
Searches for the key of a single item where the given function returns a truthy value. This behaves like
Array.findIndex(),
but returns the key rather than the positional index.

```ts
collection.findKey(user => user.username === 'Bob');
```

| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| fn | (value: Vkey: Kcollection: Collection,\<,K,V,>) =>  isK2 | ❌ | The function to test with (should return boolean) |
### public first(): undefinedV
Obtains the first value(s) in this collection.
### public firstKey(): undefinedK
Obtains the first key(s) in this collection.
### public flatMap(fn): Collection\<KT>
Maps each item into a Collection, then joins the results into a single Collection. Identical in behavior to
Array.flatMap().

```ts
collection.flatMap(guild => guild.members.cache);
```

| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| fn | (value: Vkey: Kcollection: Collection,\<,K,V,>) => Collection\<KT> | ❌ | Function that produces a new Collection |
### public forEach(callbackfn, thisArg?): [void](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/undefined)
Executes a provided function once per each key/value pair in the Map, in insertion order.



| Parameter | Type | Optional |
| ----------- | ----------- | ----------- |
| callbackfn | (value: Vkey: Kmap: Map,\<,K,V,>) => [void](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/undefined) | ❌ |
| thisArg | [any](https://www.typescriptlang.org/docs/handbook/2/everyday-types.html#any) | ✅ |
### public get(key): undefinedV
Returns a specified element from the Map object. If the value that is associated to the provided key is an object, then you will get a reference to that object and any change made to that object will effectively modify it inside the Map.



| Parameter | Type | Optional |
| ----------- | ----------- | ----------- |
| key | K | ❌ |
### public has(key): [boolean](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Boolean)
| Parameter | Type | Optional |
| ----------- | ----------- | ----------- |
| key | K | ❌ |
### public hasAll(keys): [boolean](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Boolean)
Checks if all of the elements exist in the collection.



| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| keys | [Array](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array)\<K> | ❌ | The keys of the elements to check for |
### public hasAny(keys): [boolean](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Boolean)
Checks if any of the elements exist in the collection.



| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| keys | [Array](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array)\<K> | ❌ | The keys of the elements to check for |
### public intersect(other): Collection\<KT>
The intersect method returns a new structure containing items where the keys and values are present in both original structures.



| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| other | ReadonlyCollection\<KT> | ❌ | The other Collection to filter against |
### public keyAt(index): undefinedK
Identical to Array.at().
Returns the key at a given index, allowing for positive and negative integers.
Negative integers count back from the last item in the collection.



| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| index | [number](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Number) | ❌ | The index of the key to obtain |
### public keys(): IterableIterator\<K>
Returns an iterable of keys in the map
### public last(): undefinedV
Obtains the last value(s) in this collection.
### public lastKey(): undefinedK
Obtains the last key(s) in this collection.
### public map(fn): [Array](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array)\<T>
Maps each item to another value into an array. Identical in behavior to
Array.map().

```ts
collection.map(user => user.tag);
```

| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| fn | (value: Vkey: Kcollection: Collection,\<,K,V,>) => T | ❌ | Function that produces an element of the new array, taking three arguments |
### public mapValues(fn): Collection\<KT>
Maps each item to another value into a collection. Identical in behavior to
Array.map().

```ts
collection.mapValues(user => user.tag);
```

| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| fn | (value: Vkey: Kcollection: Collection,\<,K,V,>) => T | ❌ | Function that produces an element of the new collection, taking three arguments |
### public merge(other, whenInSelf, whenInOther, whenInBoth): Collection\<KR>
Merges two Collections together into a new Collection.

```ts
// Sums up the entries in two collections.
coll.merge(
 other,
 x => ({ keep: true, value: x }),
 y => ({ keep: true, value: y }),
 (x, y) => ({ keep: true, value: x + y }),
);
```

```ts
// Intersects two collections in a left-biased manner.
coll.merge(
 other,
 x => ({ keep: false }),
 y => ({ keep: false }),
 (x, _) => ({ keep: true, value: x }),
);
```

| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| other | ReadonlyCollection\<KT> | ❌ | The other Collection to merge with |
| whenInSelf | (value: Vkey: K) => Keep\<R> | ❌ | Function getting the result if the entry only exists in this Collection |
| whenInOther | (valueOther: Tkey: K) => Keep\<R> | ❌ | Function getting the result if the entry only exists in the other Collection |
| whenInBoth | (value: VvalueOther: Tkey: K) => Keep\<R> | ❌ | Function getting the result if the entry exists in both Collections |
### public partition(fn): \[Collection\<K2V>Collection\<Exclude\<KK2>V>]
Partitions the collection into two collections where the first collection
contains the items that passed and the second contains the items that failed.

```ts
const [big, small] = collection.partition(guild => guild.memberCount > 250);
```

| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| fn | (value: Vkey: Kcollection: Collection,\<,K,V,>) =>  isK2 | ❌ | Function used to test (should return a boolean) |
### public random(): undefinedV
Obtains unique random value(s) from this collection.
### public randomKey(): undefinedK
Obtains unique random key(s) from this collection.
### public reduce(fn, initialValue?): T
Applies a function to produce a single value. Identical in behavior to
Array.reduce().

```ts
collection.reduce((acc, guild) => acc + guild.memberCount, 0);
```

| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| fn | (accumulator: Tvalue: Vkey: Kcollection: Collection,\<,K,V,>) => T | ❌ | Function used to reduce, taking four arguments; `accumulator`, `currentValue`, `currentKey`, and `collection` |
| initialValue | T | ✅ | Starting value for the accumulator |
### public reverse(): Collection\<KV>
Identical to Array.reverse()
but returns a Collection instead of an Array.
### public set(key, value): Collection\<KV>
| Parameter | Type | Optional |
| ----------- | ----------- | ----------- |
| key | K | ❌ |
| value | V | ❌ |


- [Source](https://github.com/Androz2091/discord-player/blob/cd18f37/packages/utils/src/Collection.ts#L15)
### public some(fn): [boolean](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Boolean)
Checks if there exists an item that passes a test. Identical in behavior to
Array.some().

```ts
collection.some(user => user.discriminator === '0000');
```

| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| fn | (value: Vkey: Kcollection: Collection,\<,K,V,>) => [unknown](https://www.typescriptlang.org/docs/handbook/release-notes/typescript-3-0.html#new-unknown-top-type) | ❌ | Function used to test (should return a boolean) |
### public sort(compareFunction?): Collection\<KV>
The sort method sorts the items of a collection in place and returns it.
The sort is not necessarily stable in Node 10 or older.
The default sort order is according to string Unicode code points.

```ts
collection.sort((userA, userB) => userA.createdTimestamp - userB.createdTimestamp);
```

| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| compareFunction | Comparator\<KV> | ✅ | Specifies a function that defines the sort order. If omitted, the collection is sorted according to each character's Unicode code point value, according to the string conversion of each element. |
### public sorted(compareFunction?): Collection\<KV>
The sorted method sorts the items of a collection and returns it.
The sort is not necessarily stable in Node 10 or older.
The default sort order is according to string Unicode code points.

```ts
collection.sorted((userA, userB) => userA.createdTimestamp - userB.createdTimestamp);
```

| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| compareFunction | Comparator\<KV> | ✅ | Specifies a function that defines the sort order. If omitted, the collection is sorted according to each character's Unicode code point value, according to the string conversion of each element. |
### public subtract(other): Collection\<KV>
The subtract method returns a new structure containing items where the keys and values of the original structure are not present in the other.



| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| other | ReadonlyCollection\<KT> | ❌ | The other Collection to filter against |
### public sweep(fn): [number](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Number)
Removes items that satisfy the provided filter function.



| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| fn | (value: Vkey: Kcollection: Collection,\<,K,V,>) => [unknown](https://www.typescriptlang.org/docs/handbook/release-notes/typescript-3-0.html#new-unknown-top-type) | ❌ | Function used to test (should return a boolean) |
### public tap(fn): Collection\<KV>
Runs a function on the collection and returns the collection.

```ts
collection
 .tap(coll => console.log(coll.size))
 .filter(user => user.bot)
 .tap(coll => console.log(coll.size))
```

| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| fn | (collection: Collection,\<,K,V,>) => [void](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/undefined) | ❌ | Function to execute |
### public toJSON(): [Array](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array)\<V>

### public values(): IterableIterator\<V>
Returns an iterable of values in the map
### public static combineEntries(entries, combine): Collection\<KV>
Creates a Collection from a list of entries.

```ts
Collection.combineEntries([["a", 1], ["b", 2], ["a", 2]], (x, y) => x + y);
// returns Collection { "a" => 3, "b" => 2 }
```

| Parameter | Type | Optional | Description |
| ----------- | ----------- | ----------- | ----------- |
| entries | Iterable\<\[KV]> | ❌ | The list of entries |
| combine | (firstValue: VsecondValue: Vkey: K) => V | ❌ | Function to combine an existing entry with a new one |